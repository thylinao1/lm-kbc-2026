# generated artifacts
pools/
submissions/
__pycache__/
*.pyc
.DS_Store

# competitor repos are read for method only and never vendored
refs/

# the official dataset is a separate Apache-2.0 repo; clone it, do not fork it here
dataset2026/

# private working notes, not part of the code deliverable
NEXT.local.md
.intel-reminders
notes/

# analysis scripts write their JSON here
analysis/

# LaTeX build intermediates (the .tex, .bib, .sty and final PDF are tracked)
paper/build/*.aux
paper/build/*.log
paper/build/*.out
paper/build/*.blg
paper/build/*.bcf
paper/build/*.run.xml
